//vraag eens wat het verschil is tussen "foreach" and "in", wanneer wat de beste optie is voor een situatie


let cursisten = ['Tom','Tim','bart','Els'];

//for=loopstarter
for(index in cursisten){
    document.write(index, ":",cursisten[index], "<br>");
}

